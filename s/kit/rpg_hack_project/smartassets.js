window.addEventListener('load', function () {
	Hack.on('load', function() {
		this.smartAssets = [
		{
			name: 'Warp',
			lines: [
			'// きょうぼうな スライム',
			'var enemy = new BlueSlime();',
			'enemy.locate(5, 3);']
		}
		];
	});
});